---
import GitHubActivityClient from "@components/GitHubActivityClient"

interface Props {
  profileUrl: string
  username: string
}

const { profileUrl, username } = Astro.props
---

<GitHubActivityClient client:load profileUrl={profileUrl} username={username} />
